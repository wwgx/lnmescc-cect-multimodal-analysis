# Generated Results

The analysis notebook writes tables and figures here during an authorized local run. This directory is excluded from version control because intermediate exports can contain individual-level predictions or identifiers.

Only non-identifying, manuscript-ready aggregate outputs should be deliberately added to a public release after a privacy review.
